[workspace]

[package]
name = "sdk-examples"
edition = "2021"
default-run = "sdk-examples"


[dependencies]
avail-rust = { path = "../.", default-features = false, features = ["native", "reqwest" ] }
tokio = { version = "1.45.0", features = ["rt-multi-thread", "macros"] }